# 紫藤数学刷题站 · GitHub Pages 更新说明

这个文件夹已经是完整的手机网页版，不需要安装编程软件，也不需要修改代码。

## 上传到 GitHub

1. 打开现有的 `math` 仓库。
2. 解压更新包，把解压后的所有文件上传到仓库最外层，并选择替换同名文件。上传后应能直接看到 `index.html`、`app.js`、`styles.css` 等文件。
3. 打开仓库的 **Settings → Pages**。
4. 在 **Build and deployment** 中选择 **Deploy from a branch**。
5. Branch 选择 `main`，文件夹选择 `/ (root)`，点击 **Save**。
6. 等待约1～3分钟，GitHub 会显示可访问网址，通常是 `https://你的用户名.github.io/仓库名/`。

## 手机使用

- 用手机浏览器打开 GitHub Pages 网址即可答题。
- iPhone：Safari 底部“分享” → “添加到主屏幕”。
- Android：Chrome 菜单 → “添加到主屏幕”或“安装应用”。

## 手机和电脑同步

1. 在电脑端打开左侧“手机与电脑同步”，点击“生成同步码”或自行设置至少 8 位同步码。
2. 点击“同步”，等待显示“已同步”。
3. 在手机端打开同一个网站，在“题目”面板中输入完全相同的同步码，再点击“同步”。
4. 此后两台设备每次答题都会自动保存到本机并同步到云端；换设备打开时会恢复较新的进度。

同步码相当于这份进度的钥匙，请使用不容易被猜到的组合，不要发给别人。数学站和综合测试站的进度已分开存放，使用相同同步码也不会互相覆盖。

## 错题回练

- 进入“错题回练”时，错题选项会全部恢复为空白，需要重新选择并提交。
- 重做正确后，该题会从错题数量中移除；本轮解析仍会正常显示。
- 当前题号、已完成记录、错题、收藏和计时都会自动保留。
